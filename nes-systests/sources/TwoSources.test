# name: sources/TESTDATACanNotAppearInTestName.test
# description: #503
# groups: [Tuples]

# Both queries below should pass, but the first fails.
CREATE LOGICAL SOURCE fiveTuples(field_1 UINT64 NOT NULL);
CREATE PHYSICAL SOURCE FOR fiveTuples TYPE File;
ATTACH INLINE
1
2
3
4
5

CREATE LOGICAL SOURCE sixTuples(field_1 UINT64 NOT NULL);
CREATE PHYSICAL SOURCE FOR sixTuples TYPE File;
ATTACH INLINE
1
2
3
4
5
6

CREATE SINK fiveTuplesSink(field_1 UINT64 NOT NULL)  TYPE File;
CREATE SINK sixTuplesSink(field_1 UINT64 NOT NULL)  TYPE File;

# Description: simply reads in five tuples with a single value and produces the same tuples
SELECT field_1 FROM fiveTuples INTO fiveTuplesSink;
----
1
2
3
4
5

# Description: simply reads in six tuples with a single value and produces the same tuples
SELECT field_1 FROM sixTuples INTO sixTuplesSink;
----
1
2
3
4
5
6
