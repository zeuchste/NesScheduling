CREATE LOGICAL SOURCE stream(id UINT64 NOT NULL, value UINT64 NOT NULL);
CREATE PHYSICAL SOURCE FOR stream TYPE File;
ATTACH INLINE
1,2

CREATE SINK CORRECT_SINK(id UINT64 NOT NULL)  TYPE File;
SELECT id FROM stream INTO CORRECT_SINK;
----
1

CREATE SINK INCORRECT_SINK(value UINT64 NOT NULL)  TYPE File;
SELECT id FROM stream INTO INCORRECT_SINK;
----
1
